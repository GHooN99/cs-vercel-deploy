// 로그인이 login 일때 회원가입은?
import { LoginScreen } from '@/features/users/screen/LoginScreen';

const LoginPage = () => {
  // todo 이미 로그인 되어있으면 메인 페이지로 리다이렉트
  return <LoginScreen />;
};

export default LoginPage;
